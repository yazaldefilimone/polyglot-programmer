enum color {
    reed,
    green,
    yellow,
    rgb,
}

impl color {
    fn new(reed: i64, green: i64, yellow: i64) -> Self {
        let red = color::reed;
        return red;
    }
}

fn main() {
    let new_color: color = color::new(250, 0, 0);
}
