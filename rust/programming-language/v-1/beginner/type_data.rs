fn main() {
    let x: i32 = 100; // i32 = 32bits(default) / i64 = 64bits
                      // i = signed, u= unsigned
    let x: u32 = 100; // i32 = 32bits no receive negative numbers,

    let f = 6.4; // f64 = 64 bits

    print!("{}", f)
}
